/*let tarea = document.querySelector(".text");
let botonAgregar = document.querySelector(".button");
let contenedor= document.querySelector(".container");
let formulario = document.querySelector("form")*/

/*class item{ 
 constructor(nuevaTarea) {
    this.crearDiv(nuevaTarea)
    }
};*/
    

/*tarea.addEventListener("click",function(e){*/
    class item{ 
        constructor(tarea) {
           this.crearDiv(tarea)
           crearDiv(nuevaTarea)
    /*let dato = texto.value;
    let div = document.createElement("regar las plantas")
    nuevaTarea.innerText = dato;
    div.appendChild(div);*/
   }
}



/*formulario.addEventListener("submit",function(e){
    e.preventDefault();
})*/